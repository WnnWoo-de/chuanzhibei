# 🌤️ 天气查询系统 - 快速启动指南

## 📋 前置要求

- Node.js 14+ 
- npm 或 yarn
- 和风天气API密钥（已在.env中配置）

## 🚀 快速启动

### 步骤 1: 启动后端服务

```bash
cd grennn_backend

# 安装依赖（如果还未安装）
npm install

# 启动开发服务器
npm run dev
```

✅ 后端服务将在 `http://localhost:3000` 启动

### 步骤 2: 启动前端应用

在新的终端窗口中：

```bash
cd grennn_web

# 安装依赖（如果还未安装）
npm install

# 启动开发服务器
npm run dev
```

✅ 前端应用将在 `http://localhost:5173` 启动

### 步骤 3: 访问天气查询页面

打开浏览器访问：
```
http://localhost:5173
```

导航到 **天气查询** 页面，输入城市名称开始查询。

---

## 🧪 测试API

### 方法 1: 使用测试脚本

```bash
cd grennn_backend
node test-weather-api.js
```

这将自动测试多个城市的天气数据。

### 方法 2: 使用 curl

```bash
# 查询北京天气
curl "http://localhost:3000/api/v1/weather/query?city=北京"

# 查询上海天气
curl "http://localhost:3000/api/v1/weather/query?city=上海"
```

### 方法 3: 使用 Postman

1. 打开 Postman
2. 创建新的 GET 请求
3. URL: `http://localhost:3000/api/v1/weather/query?city=北京`
4. 点击 Send

---

## 📁 项目文件说明

### 后端文件

| 文件 | 说明 |
|------|------|
| `controllers/weatherController.js` | 天气业务逻辑处理 |
| `routes/weather.js` | 天气API路由定义 |
| `.env` | 环境变量配置（API密钥等） |
| `test-weather-api.js` | API测试脚本 |

### 前端文件

| 文件 | 说明 |
|------|------|
| `src/views/weather/WeatherQuery.vue` | 天气查询UI组件 |
| `src/services/weatherService.js` | 天气API服务函数 |

---

## 🎨 UI 特性

✨ **设计特点：**
- 极简主义风格，与项目整体风格一致
- 网格背景设计
- 清晰的信息层级
- 响应式布局（移动端友好）
- 流畅的加载动画

📊 **功能模块：**
1. **城市搜索** - 输入城市名称查询
2. **实时天气** - 温度、湿度、风力等信息
3. **空气质量** - AQI指数和污染物监测
4. **5天预报** - 未来天气趋势
5. **污染物趋势** - 历史数据分析

---

## 🔧 配置说明

### 环境变量 (.env)

```env
# 天气API配置
QWEATHER_API_KEY=084248a962674ad9b44644ea654de3ea
QWEATHER_HOST=devapi.qweather.com

# 服务器配置
PORT=3000
```

**说明：**
- `QWEATHER_API_KEY`: 和风天气API密钥
- `QWEATHER_HOST`: API主机地址（免费版使用devapi）
- `PORT`: 后端服务端口

---

## 📡 API 端点

### 天气查询

**请求：**
```
GET /api/v1/weather/query?city=城市名称
```

**参数：**
- `city` (string, required) - 城市名称（支持中文、英文、拼音）

**响应示例：**
```json
{
  "city": {
    "id": "101010100",
    "name": "北京",
    "adm1": "北京",
    "adm2": "朝阳区"
  },
  "now": {
    "temp": 15,
    "feelsLike": 13,
    "text": "晴",
    "humidity": 65,
    "windDir": "东北风",
    "windScale": "3"
  },
  "forecast": [...],
  "airQuality": {...}
}
```

---

## ⚠️ 常见问题

### Q: 后端启动失败？
**A:** 检查以下几点：
- Node.js 是否已安装：`node --version`
- 依赖是否已安装：`npm install`
- 端口 3000 是否被占用
- .env 文件是否存在且配置正确

### Q: 前端无法连接到后端？
**A:** 
- 确保后端服务已启动
- 检查 CORS 配置是否正确
- 检查前端的 API 基础 URL 配置

### Q: 查询返回 404 错误？
**A:**
- 检查城市名称是否正确
- 尝试使用其他城市名称
- 检查和风天气API是否可用

### Q: API 密钥无效？
**A:**
- 访问 [和风天气控制台](https://console.qweather.com/) 获取新密钥
- 更新 .env 文件中的 `QWEATHER_API_KEY`
- 重启后端服务

---

## 🚀 生产部署

### 后端部署

```bash
# 构建
npm install --production

# 启动
npm start
```

### 前端部署

```bash
# 构建
npm run build

# 输出目录：dist/
```

---

## 📚 相关文档

- [天气系统实现文档](./天气系统实现文档.md) - 详细的技术文档
- [天气API.md](./天气api.md) - 原始API文档
- [和风天气官方文档](https://dev.qweather.com/) - 官方API参考

---

## 💡 下一步

1. ✅ 启动后端和前端服务
2. ✅ 测试天气查询功能
3. ✅ 自定义UI样式（如需要）
4. ✅ 添加更多功能（如城市收藏、预警通知等）
5. ✅ 部署到生产环境

---

**需要帮助？** 查看详细的技术文档或检查控制台错误信息。
