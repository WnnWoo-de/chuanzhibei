# 项目运行方法

## 1. 安装依赖
```bash
cd grennn_backend
npm install

cd ../grennn_web
npm install
```

## 2. 配置环境变量
在项目grennn_backend目录新建 `.env` 文件，内容如下：
```
# 数据库配置
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASS=数据库密码
DB_NAME=greenn_web

# JWT
JWT_SECRET=your_jwt_secret_key_here

# 服务器
PORT=3000
FRONTEND_URL=http://localhost:5173
CALLBACK_URL_BASE=http://localhost:3000

# AI 服务
AI_SERVICE=SILICONFLOW
GLM_API_KEY=your_glm_api_key_here
GLM_MODEL=glm-4-flash
SILICONFLOW_API_KEY=sk-qdxkefntzaiahyvyjvbwyxzscigsmonainooyfgruynlmcdc
```

## 3. 初始化数据库
```bash
cd grennn_backend
node scripts/init_db.js
```

## 4. 启动项目
```bash
# 启动后端
cd grennn_backend
npm run dev

# 启动前端（新终端）
cd grennn_web
npm run dev
```
## 测试Fast API
```
http://127.0.0.1:8003/docs
```