# Cloudflare Pages 配置 - 最简格式
name = "chuanzhibei"
compatibility_date = "2026-04-06"

# Pages 构建配置
pages_build_output_dir = "grennn_web/dist"

# 构建命令
[build]
command = "cd grennn_web && npm ci && npm run build"
