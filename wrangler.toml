# Cloudflare Pages 配置
name = "chuanzhibei"
compatibility_date = "2026-04-06"

# Pages 构建配置
[build]
command = "cd grennn_web && npm install && npm run build"
upload_dir = "grennn_web/dist"

# Pages 静态资源配置
[assets]
not_found_handling = "single-page-application"
