# 天气查询系统 - 完整实现指南

## 项目结构

```
grennn_backend/
├── controllers/weatherController.js    # 天气业务逻辑
├── routes/weather.js                   # 天气路由
├── .env                                # 环境变量配置
└── server.js                           # 主服务器文件

grennn_web/
├── src/
│   ├── views/weather/WeatherQuery.vue  # 天气查询UI组件
│   ├── services/weatherService.js      # 前端API服务
│   └── utils/api.js                    # HTTP请求工具
```

## 后端实现

### 1. 环境变量配置 (.env)

```env
QWEATHER_API_KEY=084248a962674ad9b44644ea654de3ea
QWEATHER_HOST=devapi.qweather.com
PORT=3000
```

### 2. 天气控制器 (weatherController.js)

核心功能：
- 城市地理位置查询（GeoAPI）
- 实时天气数据获取
- 5天天气预报
- 空气质量监测
- 污染物趋势分析

**API端点：** `GET /api/v1/weather/query?city=北京`

**响应数据结构：**

```javascript
{
  city: {
    id: "101010100",
    name: "北京",
    adm1: "北京",
    adm2: "朝阳区",
    country: "中国"
  },
  now: {
    temp: 15,              // 温度
    feelsLike: 13,         // 体感温度
    text: "晴",            // 天气状况
    humidity: 65,          // 湿度
    windDir: "东北风",     // 风向
    windScale: "3",        // 风力等级
    pressure: 1013,        // 气压
    vis: 10                // 能见度
  },
  forecast: [             // 5天预报
    {
      date: "2025-03-20",
      textDay: "晴",
      textNight: "晴",
      tempMax: 20,
      tempMin: 10,
      precip: 0,
      humidity: 60
    }
    // ... 更多天数
  ],
  airQuality: {
    aqi: 45,               // 空气质量指数
    level: "优",           // 等级
    advice: "空气质量优，适合外出活动。",
    pollutants: [          // 污染物数据
      {
        key: "pm2p5",
        name: "PM2.5",
        unit: "μg/m³",
        value: 25
      }
      // ... 其他污染物
    ],
    trend: {               // 污染物趋势
      pm2p5: [
        { date: "2025-03-20", value: 25 },
        { date: "2025-03-21", value: 28 }
      ]
      // ... 其他污染物趋势
    }
  }
}
```

### 3. 错误处理

| 状态码 | 错误信息 | 原因 |
|--------|--------|------|
| 400 | 请提供城市名称 | 缺少city参数 |
| 404 | 未找到该城市 | 城市不存在 |
| 500 | 天气服务未配置 | 缺少API_KEY |
| 502 | 天气数据获取失败 | API请求失败 |

## 前端实现

### 1. 天气服务 (weatherService.js)

```javascript
import { queryWeatherByCity } from '@/services/weatherService'

// 使用示例
const result = await queryWeatherByCity('北京')
if (result.ok) {
  console.log(result.data)  // 天气数据
} else {
  console.error(result.message)  // 错误信息
}
```

### 2. UI组件 (WeatherQuery.vue)

**功能特性：**
- 城市搜索输入框
- 实时天气显示（温度、湿度、风力）
- 空气质量指数圆形进度条
- 5天天气预报卡片
- 污染物监测面板
- 加载状态和错误处理
- 响应式设计

**UI风格：**
- 极简主义设计
- 网格背景
- 单色调配色
- 清晰的排版层级
- 适配项目整体风格

## 使用流程

### 后端启动

```bash
cd grennn_backend
npm install
npm run dev
```

服务器将在 `http://localhost:3000` 启动

### 前端开发

```bash
cd grennn_web
npm install
npm run dev
```

前端将在 `http://localhost:5173` 启动

### API调用流程

1. 用户在WeatherQuery.vue输入城市名称
2. 点击"查询"按钮触发handleQuery()
3. 调用queryWeatherByCity()服务函数
4. 发送GET请求到 `/api/v1/weather/query?city=城市名`
5. 后端weatherController处理请求：
   - 调用和风天气GeoAPI获取城市ID
   - 并行请求实时天气、预报、空气质量数据
   - 数据处理和格式化
   - 返回完整的天气数据
6. 前端接收数据并渲染UI

## 和风天气API说明

### 免费版本限制

- **GeoAPI**: 城市查询，无限制
- **天气API**: 实时天气、预报，免费版支持
- **空气质量API**: 实时AQI、5天趋势，免费版支持
- **更新频率**: 实时数据每小时更新

### API密钥获取

1. 访问 [和风天气控制台](https://console.qweather.com/)
2. 注册账号并创建项目
3. 获取API_KEY
4. 配置到.env文件中

### 请求限制

- 免费版: 每天50,000次请求
- 生产环境建议升级付费版本

## 数据流图

```
用户输入城市
    ↓
WeatherQuery.vue (handleQuery)
    ↓
weatherService.js (queryWeatherByCity)
    ↓
HTTP GET /api/v1/weather/query
    ↓
weatherController.js (queryWeather)
    ↓
和风天气API
├── GeoAPI (城市查询)
├── Weather API (实时天气 + 5天预报)
└── Air Quality API (AQI + 污染物)
    ↓
数据处理和格式化
    ↓
返回JSON响应
    ↓
前端渲染UI
```

## 常见问题

### Q: 如何更换API密钥？
A: 修改 `.env` 文件中的 `QWEATHER_API_KEY` 值，重启服务器。

### Q: 为什么查询失败？
A: 检查以下几点：
1. API密钥是否正确配置
2. 城市名称是否正确（支持中文、英文、拼音）
3. 网络连接是否正常
4. 和风天气API是否可用

### Q: 如何添加更多污染物监测？
A: 在weatherController.js中的pollutants数组添加新项目，和风天气支持的污染物包括：pm2p5, pm10, o3, no2, so2, co

### Q: 如何自定义AQI等级颜色？
A: 在WeatherQuery.vue中修改aqiColor计算属性的颜色值。

## 性能优化建议

1. **缓存策略**: 添加Redis缓存，减少API调用
2. **请求合并**: 使用Promise.all并行请求多个API
3. **数据压缩**: 启用gzip压缩响应数据
4. **CDN加速**: 部署到CDN加速静态资源
5. **限流保护**: 添加速率限制防止滥用

## 安全建议

1. **API密钥保护**: 不要在前端暴露API密钥
2. **HTTPS**: 生产环境使用HTTPS
3. **CORS配置**: 限制允许的来源
4. **输入验证**: 验证城市名称输入
5. **错误信息**: 不要返回敏感的错误信息

## 扩展功能建议

1. 添加城市搜索建议（自动完成）
2. 收藏常用城市
3. 天气预警通知
4. 历史天气数据查询
5. 多语言支持
6. 天气分享功能
