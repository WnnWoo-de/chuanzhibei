# -*- coding: utf-8 -*-
import os
path = r'c:\Users\wnnw\Desktop\1111111\bisai\GreenSight AI Web项目.md'

part2 = """
---

## 二、项目亮点

### 2.1 AI 深度融合，不只是「展示」

- **旧物重构 AI 识别**：用户上传图片后，后端调用多模态 AI 模型进行材质分析，自动生成含难度评级、碳减排估算的多方案改造建议。前端动画展示「扫描→识别→计算」完整流程，极具科技感。
- **垃圾智能分类识别**：上传图片，AI 识别垃圾类别（可回收物/有害垃圾/厨余垃圾/其他垃圾），给出置信度评分与处理建议，识别结果持久化存入数据库，支持历史查询。
- **流式 AI 对话（SSE）**：聊天界面采用 Server-Sent Events 流式渲染，AI 回复逐字打印，配合打字光标动画，支持**停止生成**和**重新生成**，体验媲美主流 AI 产品。
- **碳足迹 AI 分析**：结合交通、用电、饮食等月度数据，AI 计算个人 CO₂ 排放量，分项展示各类碳排放占比，给出针对性减排建议，历史记录持久化存储支持趋势对比。
- **离线模拟兜底**：AI API 不可用时自动切换本地模拟响应（内置 12+ 类环保问答），保障演示稳定性。

### 2.2 游戏化激励机制

- **积分 + 成就徽章体系**：完成旧物重构、垃圾识别、发帖、点赞、参与志愿活动等均可获得积分，解锁专属成就徽章。平台内置 **11 枚预设成就**，含 common / rare / epic / legendary 四档稀有度。
- **志愿者活动系统**：平台发布线下环保志愿活动（清洁/植树/回收/宣教），用户报名参与后系统发放积分，活动状态实时更新。
- **社区贡献排行榜**：展示积分 Top 用户排名，营造良性竞争氛围。
- **等级成长系统**：积分驱动等级晋升，成就页面进度条直观展示距下一等级差距。

### 2.3 精心设计的前端交互体验

- **全局可折叠侧边栏**：支持锚点滚动与路由跳转双模式，含 ESC 键关闭、移动端遮罩层、活跃项高亮指示条。
- **GSAP 滚动动画**：ScrollTrigger 为各区块入场动画精确编排，动画结束后清除内联样式（`clearProps: 'all'`）不影响 CSS hover 效果。
- **Hero 轮播系统**：自动播放（5秒）、手动切换、暂停/播放控制，配合 `slide-fade` 过渡动画与动态绿色光晕。
- **无缝 Marquee 字幕**：纯 CSS `@keyframes` 实现双组内容无缝循环，悬停自动暂停。
- **天气专业图标**：接入 `qweather-icons`（v1.8.0）精准展示各天气状态矢量图标。
- **Markdown 渲染**：`marked` 库实时渲染 AI 回复，支持粗体、列表、代码块等格式，安全过滤 HTML 注入。

### 2.4 工程化与安全性

- **JWT 全链路认证**：所有业务接口均通过自定义 JWT 中间件鉴权，Token 过期前端自动感知（401）并触发登出。
- **第三方 OAuth 登录**：集成 Passport.js，支持 Google OAuth 2.0 和 Microsoft OAuth 一键登录。
- **安全中间件**：`helmet` 安全响应头 + `cors` 跨域 + `bcryptjs` 密码哈希 + `multer` 文件类型/大小（5MB）限制。
- **数据双重持久化**：MySQL + Sequelize ORM 数据库级持久化，`localStorage` 本地缓存双重保障，页面刷新数据不丢失。
- **AbortController 流控**：AI 流式请求 60 秒超时自动中断，用户主动停止立即取消网络请求，防止资源泄漏。

---

## 三、Web 网页使用情况概述

### 3.1 页面结构总览

| 路由路径 | 页面名称 | 主要功能 |
|---|---|---|
| `/` | 首页 | 项目介绍轮播、核心功能导航、精选推荐、动态资讯 |
| `/auth/login` | 登录页 | 账号密码登录、Google/Microsoft OAuth 登录 |
| `/auth/register` | 注册页 | 用户注册（邮箱格式与密码强度校验） |
| `/reconstruction` | 旧物重构 | 拖拽上传图片、AI 分析动画、多方案改造建议 |
| `/chat` | AI 助手（绿芽） | 流式智能对话、快捷提问、聊天历史、停止/重生成 |
| `/chat/waste-recognition` | 垃圾识别 | 图片上传、AI 分类识别、处理建议、积分奖励 |
| `/chat/carbon-footprint` | 碳足迹分析 | 月度数据输入、AI 碳排计算、减排建议 |
| `/community` | 社区活动 | 动态发布、图片上传、点赞、贡献排行榜 |
| `/achievements` | 成就系统 | 积分总览、等级进度条、11枚徽章解锁状态 |
| `/volunteer` | 志愿者活动 | 活动列表、报名参与、积分奖励 |
| `/weather` | 天气查询 | 城市搜索、GPS 定位、实时天气、5天预报、AQI |
| `*` | 404 页面 | 友好的「页面未找到」提示 |

### 3.2 响应式设计

- 全站采用 **Tailwind CSS** 响应式断点（`md:`、`lg:` 等），所有核心页面均针对移动端（≤768px）和桌面端（≥1024px）进行专项适配。
- 侧边栏在移动端以抽屉方式弹出，附带半透明遮罩（`backdrop-blur-sm`），触摸遮罩或按 ESC 键可关闭。
- 聊天界面的侧边信息栏在移动端简化为顶部状态条，节省屏幕空间。
- 社区页贡献榜在移动端以折叠摘要形式展示，桌面端展开完整榜单。

### 3.3 全局交互规范

- **路由进度条**：NProgress 在路由切换时展示顶部进度条，消除切换卡顿感。
- **操作反馈**：所有异步操作通过 Element Plus `ElMessage` 弹出成功/失败/警告提示。
- **加载骨架屏**：社区列表、排行榜等数据加载时展示 `SkeletonLoader` 脉冲占位动画，避免布局抖动。
- **统一视觉语言**：全站以 `#F5F5F0`（暖白）为背景色，`#2E7D32`（森林绿）为主题色，网格线装饰背景，风格简洁克制。
- **自定义滚动条**：全站统一定制细款半透明滚动条（宽度 6px），聊天快捷提问区采用隐藏滚动条横向滑动。
"""

with open(path, 'a', encoding='utf-8') as f:
    f.write(part2)
print('part2 done')
